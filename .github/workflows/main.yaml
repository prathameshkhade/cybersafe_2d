name: Build and Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Accept Android SDK licenses & install NDK
        run: |
          SYSTEM_SDK="/usr/local/lib/android/sdk"
          echo "ANDROID_SDK_ROOT=$SYSTEM_SDK" >> $GITHUB_ENV
          yes | $SYSTEM_SDK/cmdline-tools/latest/bin/sdkmanager --licenses || true
          $SYSTEM_SDK/cmdline-tools/latest/bin/sdkmanager "ndk;29.0.13599879"

      - name: Get dependencies
        run: flutter pub get

      - name: Decode Keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/keystore.jks

      - name: Create key.properties
        run: |
          echo "storeFile=keystore.jks"                        > android/key.properties
          echo "storePassword=${{ secrets.STORE_PASSWORD }}"  >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}"            >> android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}"      >> android/key.properties

      - name: Build APKs (split per ABI)
        run: flutter build apk --release --split-per-abi

      - name: Rename APKs
        run: |
          cd build/app/outputs/flutter-apk
          mv app-armeabi-v7a-release.apk  "CyberSafe2D-armeabi-v7a-release.apk"
          mv app-arm64-v8a-release.apk    "CyberSafe2D-arm64-v8a-release.apk"
          mv app-x86_64-release.apk       "CyberSafe2D-x86_64-release.apk"

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: android-builds
          path: build/app/outputs/flutter-apk/*.apk

  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Get dependencies
        run: flutter pub get

      - name: Build iOS Release
        run: flutter build ios --release --no-codesign

      - name: Zip iOS Build
        run: zip -r CyberSafe2D-ios.zip build/ios/iphoneos/

      - name: Upload iOS Build
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: CyberSafe2D-ios.zip

  release:
    needs: [build-android, build-ios]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Download Android Builds
        uses: actions/download-artifact@v4
        with:
          name: android-builds
          path: artifacts/android

      - name: Download iOS Build
        uses: actions/download-artifact@v4
        with:
          name: ios-build
          path: artifacts/ios

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: CyberSafe 2D - v${{ github.run_number }}
          files: |
            artifacts/android/*.apk
            artifacts/ios/CyberSafe2D-ios.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
